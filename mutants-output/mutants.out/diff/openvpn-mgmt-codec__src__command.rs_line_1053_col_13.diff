--- openvpn-mgmt-codec/src/command.rs
+++ delete match arm "exit" in <impl FromStr for OvpnCommand>::from_str
@@ -1045,17 +1045,17 @@
             "raw-ml" => {
                 if args.is_empty() {
                     return cmd_err("usage: raw-ml <command>");
                 }
                 Ok(Self::RawMultiLine(args.to_string()))
             }
 
             // --- Lifecycle ---
-            "exit" => Ok(Self::Exit),
+             /* ~ changed by cargo-mutants ~ */
             "quit" => Ok(Self::Quit),
 
             // --- Fallback: send as raw command ---
             _ => Ok(Self::Raw(line.to_string())),
         }
     }
 }
 
