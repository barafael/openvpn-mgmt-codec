--- openvpn-mgmt-codec/src/status.rs
+++ delete ! in parse_client_statistics
@@ -557,17 +557,17 @@
     if !found_tcp_read {
         return Err(ParseStatusError::MissingStatisticsKey("TCP/UDP read bytes"));
     }
     if !found_tcp_write {
         return Err(ParseStatusError::MissingStatisticsKey(
             "TCP/UDP write bytes",
         ));
     }
-    if !found_auth_read {
+    if  /* ~ changed by cargo-mutants ~ */found_auth_read {
         return Err(ParseStatusError::MissingStatisticsKey("Auth read bytes"));
     }
 
     Ok(stats)
 }
 
 #[cfg(test)]
 mod tests {
