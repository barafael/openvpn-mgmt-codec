--- openvpn-mgmt-codec/src/version_info.rs
+++ delete ! in VersionInfo::parse
@@ -57,17 +57,17 @@
             // Future-proof: accept any line starting with "management"
             // that contains "version" followed by a number.
             if management_version.is_none()
                 && lower.starts_with("management")
                 && lower.contains("version")
             {
                 management_version = line
                     .rsplit(|chr: char| !chr.is_ascii_digit())
-                    .find(|part| !part.is_empty())
+                    .find(|part|  /* ~ changed by cargo-mutants ~ */part.is_empty())
                     .and_then(|part| {
                         part.parse()
                             .inspect_err(|error| {
                                 tracing::warn!(%error, part, "non-numeric management version")
                             })
                             .ok()
                     });
             } else if lower.starts_with("openvpn version") {
