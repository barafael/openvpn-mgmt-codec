--- openvpn-mgmt-codec/src/stream_mode.rs
+++ replace <impl FromStr for StreamMode>::from_str -> Result<Self, Self::Err> with Ok(Default::default())
@@ -49,26 +49,17 @@
     }
 }
 
 impl FromStr for StreamMode {
     type Err = ParseStreamModeError;
 
     /// Parse a stream mode string: `on`, `off`, `all`, `on all`, or a number.
     fn from_str(input: &str) -> Result<Self, Self::Err> {
-        match input {
-            "on" => Ok(Self::On),
-            "off" => Ok(Self::Off),
-            "all" => Ok(Self::All),
-            "on all" => Ok(Self::OnAll),
-            other => other
-                .parse::<u32>()
-                .map(Self::Recent)
-                .map_err(|_| ParseStreamModeError(input.to_string())),
-        }
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use test_case::test_case;
 
